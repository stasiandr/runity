# Text as LF on every machine: a .scrimport stores a hash of its source's
# bytes, and a CRLF checkout would change every one of them.
* text=auto eol=lf

# The example project's scenes and prefabs merge by entity and field.
# `cargo run -p scrap-cli -- git-setup examples/valley` turns the driver on.
examples/valley/scenes/**/*.ron merge=scrap
examples/valley/prefabs/**/*.prefab merge=scrap
examples/kitchen/** !text !filter !merge !diff
