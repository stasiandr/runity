[package]
name = "scrap-discord"
description = "scrap's Discord presence: what a player is doing, shown on their Discord profile through the Discord client beside the game."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
discord-rich-presence = "0.2"

[dev-dependencies]
scrap-core = { path = "../scrap-core" }
