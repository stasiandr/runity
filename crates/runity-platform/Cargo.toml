[package]
name = "runity-platform"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Windowing and input for runity, talking to the OS directly. No dependencies."

[dependencies]
