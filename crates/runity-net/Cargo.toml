[package]
name = "runity-net"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Length-prefixed frame transport for runity: loopback and TCP behind one trait."

[dependencies]
runity-sim = { path = "../runity-sim", version = "0.1.0" }
