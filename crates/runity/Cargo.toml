[package]
name = "runity"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "runity: a dependency-free game engine in Rust."

[dependencies]
runity-ai = { path = "../runity-ai", version = "0.1.0" }
runity-audio = { path = "../runity-audio", version = "0.1.0" }
runity-math = { path = "../runity-math", version = "0.1.0" }
runity-render = { path = "../runity-render", version = "0.1.0" }
runity-net = { path = "../runity-net", version = "0.1.0" }
runity-physics = { path = "../runity-physics", version = "0.1.0" }
runity-serialize = { path = "../runity-serialize", version = "0.1.0" }
runity-platform = { path = "../runity-platform", version = "0.1.0" }
runity-terrain = { path = "../runity-terrain", version = "0.1.0" }
runity-core = { path = "../runity-core", version = "0.1.0" }
