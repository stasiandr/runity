# Следующие шаги

Список дел, а не решения: решения — в [DNA.md](DNA.md), как сделано — в
[stack.md](stack.md). Сделал пункт — отметь `[x]`; пункт, который упёрся в
постулат или открытый вопрос DNA, называет его.

# Привести код к DNA

Решения от 2026-09-23 («Голое ядро и наборы», «Модуль — исходник»,
«Видеокарта — модуль `gpu`», «Геометрия — базовый модуль» и остальные).
Сегодня `runity` — один крейт, и рендер, сеть, UI, анимация и навигация в нём
без флагов.

- [ ] Разрезать `runity` на ядро и модули-крейты
  - [ ] Первой — физика: шов почти готов (`physics` уже фича), на ней
        проверяются манифест и фазы
  - [ ] `geometry`: меш как данные, границы, лучи, простые фигуры;
        `MeshAsset` уезжает из ядра
  - [ ] `gpu`: устройство, поверхность, текстуры, шейдеры
  - [ ] `render` и `ui` поверх `gpu`
  - [ ] Навигация, анимация, звук, сеть, Steam, террейн, scatter, блокаут —
        каждый своим модулем
  - [ ] Голое ядро собирается и проходит тесты без GPU (сервер, тест, MCP)
- [ ] Модуль как единица поставки
  - [ ] Манифест модуля: имя, версия, совместимость с ядром, зависимости, что
        приносит в рантайм и что только в редактор
  - [ ] Модуль без кода — только ассеты
  - [ ] Список модулей проекта в `runity.ron`; зависимости `Cargo.toml`
        выводятся из него; `check` ловит расхождение
  - [ ] Типы ассетов регистрируют модули
  - [ ] Официальные модули идут поездом релизов с ядром
- [ ] `runity new`: голый, базовый, полный набор или темплейт
  - [ ] Состав базового набора — из мерки Flappy Bird (картинка, ввод, счёт,
        звук, столкновения); решить, физика в нём или пересечения в `geometry`
  - [ ] Темплейты — проекты в репозитории движка, CI их собирает
- [ ] Цикл по фазам PlayerLoop: инициализация, начало кадра, FixedUpdate
      (`step`), Update, LateUpdate, конец кадра с рендером (`frame`); модуль
      вставляет системы в фазу
- [ ] Ввод как Unity Input System: в ядре — события устройств; действия, карты,
      привязки, схемы управления — модуль; `input.ron` — его частный случай
- [ ] Компонент отключённого модуля хранится текстом, переживает сохранение,
      `check` его называет
- [ ] Редактор как набор модулей
  - [ ] Голый редактор: иерархия, инспектор, Project, консоль
  - [ ] Точки расширения: панели, меню, инструменты, гизмо, импортёры, формы
        инспектора
  - [ ] Действие модуля регистрируется один раз — его видят меню и агент
        (MCP-инструмент)
- [ ] Рендер из отключаемых проходов: каждый проход выключается при сборке
      (его нет в бинаре) и в рантайме (настройки графики); минимум — меши без
      света
- [ ] Два профиля сборки игры: на скорость (по умолчанию) и на размер
- [ ] Не закрывать веб: запуск без блокировки потока, чтение данных через шов,
      часы цикла без `std::time::Instant`, ядро в одном потоке

# Графовые редакторы

Решение «Графовые редакторы» (DNA, 2026-09-23), постулат 5.

- [x] Переход аниматора — в состоянии, из которого выходит; Any State — `any`
- [x] Раскладка аниматора только автоматическая, `.runity/animators.ron` нет
- [ ] Громкие проверки графа в `check` и `problems`: недостижимое состояние,
      тупик, параметр, который игра не ставит, затенённый переход
- [ ] Прогон графа без игры: сценарий параметров → ожидаемые состояния, как
      тест-данные
- [ ] След переходов во время игры для агента (`game_state`): из какого в
      какое, когда и по какому условию
- [ ] Граф с наложенным диффом — ревью правки агента и визуальный мёрж
- [ ] Один виджет графа в `runity-ui`; домен — схема, которую приносит модуль
- [ ] Общие MCP-инструменты графа: прочитать, соединить, переименовать с
      правкой ссылок
- [ ] Шейдер-граф, граф частиц, диалоги — на том же виджете

# Перенос Dacha Simulator

Что нужно, чтобы `~/personal/dacha-simulator` (Unity 6, URP, до 16 игроков,
Steam) жил на runity. Порядок — по риску: сначала то, что может сделать
перенос бессмысленным.

## 1. Физика выдерживает «Дачу»

Геймплей «Дачи» — это поведение PhysX: `MovePosition`, правило трения,
нормали контактов как правда о земле, ломающиеся суставы. На rapier это
будет ощущаться иначе — и это нормально: физика не обязана работать один в
один, как в «Даче», её подтьюним заново (решение 2026-09-23). Поэтому
сравнения «ощущается ли как PhysX» нет; нужны возможности, на которых
держится геймплей.

- [x] Несколько изолированных физических миров в одном процессе (мир на пира,
      как в стенде «Дачи») — `PhysicsWorld` — значение, тест держит, что два
      мира не трогают друг друга и детерминированы
- [ ] Физический стенд: 13 сценариев «Дачи» (`Assets/Tests/PlayMode/PhysicsBench/`)
      на лестнице ухудшающихся связей, её метрики — правда против одиночного
      прогона, ошибка картинки, детекторы запуска, байты в секунду
- [x] ~~Сравнить с PhysX~~ — не нужно: физику тьюним заново
- [x] Суставы, которые ломаются: `joint_break` (ньютоны) на строке,
      `JointBroken` на сущности, `PhysicsWorld::broken()` — событие
- [x] Пружины и приводы: `Joint::Spring` (SpringJoint), мотор и пределы
      шарнира и ползунка меняются во время игры на месте, без пересборки
      сустава
- [x] Смена «динамическое ↔ кинематическое» с сохранением скорости: тело то
      же, скорость сохраняется (или берётся из движения, если его двигали)

## 2. Контент переезжает сам

- [x] Импортёр Unity-проекта (батарейка для любого, кто уходит с Unity,
      постулат 8): `runity import-unity`, карточка docs/unity-import.md
  - [x] `.unity` и `.prefab` → сцены и префабы в RON, со стабильными ID
  - [x] Материалы URP Lit → `.rmat`
  - [x] Animator Controller → `animators/`
  - [x] Модели FBX (270 штук) и клипы в них — через Blender в GLB
  - [x] Коллайдеры, тела и суставы → строки сцены (ConfigurableJoint — пока
        шаровым)
- [ ] Свои шейдеры на материал (сегодня материал — числа одного шейдера)
- [ ] Переписать на WGSL 20 Shader Graph и 6 HLSL «Дачи»: вода, зеркало,
      стекло, голограмма, обводка, песок, пыль и остальные

## 3. Чего не хватает движку

- [ ] Анимация
  - [ ] 2D-смешивание по направлению (freeform directional blend tree)
  - [ ] Клипы Mixamo на свой скелет: перенос гуманоида или перепекание при
        импорте
  - [ ] Меш, прикреплённый к кости (не нашёл — проверить)
  - [ ] Клип, прокручиваемый параметром (стадии стройки по Health)
- [ ] Рендер
  - [ ] Планарное зеркало: вторая камера в текстуру со своей маской слоёв
  - [ ] Меш, который меняется каждый кадр (поверхность воды, тросы)
  - [ ] Lens flare на лампе, как `LensFlareComponentSRP` (экранный уже есть)
  - [ ] Volume, смешивающийся по положению (сейчас `post` на всю сцену)
- [x] Частицы — самый большой разрыв в графике: 346 систем Shuriken
  - [x] Текстуры и материалы частиц: `model:` и `material:` эмиттера —
        ссылки; цвет и размер за жизнь (`end_color`, `end_size`)
  - [x] Вытягивание по скорости (`stretch`), локальное и мировое
        пространство (`local`), эмиссия из кода: `Emitting::emit`,
        `play`/`stop`, вспышки по времени (`bursts`), один раз (`once`) и
        по команде (`waits`)
  - [x] Импорт систем Shuriken: Main, Emission со вспышками, Shape (конус,
        сфера), Size и Color over Lifetime, Renderer (меш, растяжка,
        материал). Шум, столкновения, следы, скорость за жизнь — в отчёте
  - [x] Частицы лицом к камере (`facing`, billboard Unity)
  - [ ] Прозрачность по альфе цвета за жизнь (сейчас цвет — RGB)
- [ ] Звук
  - [x] Группы громкости микшера (в «Даче» Master, Music, SFX):
        `Audio::play_in`, `set_group_volume` — громкость меняется и у
        того, что уже звучит; master тоже
  - [ ] Голос рядом с персонажем (proximity voice)
- [ ] UI
  - [ ] Виджеты под ~14 экранов «Дачи»: вкладки, списки, прокрутка, ввод
        текста
  - [ ] Навигация геймпадом
  - [ ] UI в мире, на который нажимают прицелом
- [ ] Сеть (по «Отложенному» в stack.md)
  - [ ] Заявки при приближении и заявки на обе половины сустава
  - [ ] Группы сущностей (игрок с двумя руками)
  - [ ] Передача скорости телу при смене владельца
  - [ ] Синхронизация часов
  - [ ] Steam-транспорт проверен вживую
- [x] Сейвы: надгробия удалённых объектов сцены (`gone`), ротация
      (`SaveGame::write_rotating`: 3 сейва и копия на восстановление,
      `read_newest` находит новейший читаемый)

## 4. Сервисы и сборка

- [ ] Steam: голос, достижения, аватары, rich presence, пути под Auto-Cloud
- [ ] Отчёты о падениях (Sentry), аналитика (GameAnalytics), Discord rich
      presence
- [ ] Сборка под Steam: депо Windows и macOS, универсальный бинарь macOS с
      подписью, выкладка только в ветку `playtest`

## 5. Инструменты

Окна, на которых держится разработка «Дачи»; Multiplayer Play Mode и плохая
связь в runity уже есть.

- [x] Инспектор сети — вкладка Network: у каждого игрока, кто чем владеет,
      копия это или своё, последний тик и как давно (отчёт игры несёт
      `diagnostics`)
- [x] Дифф миров — вкладка World Diff: игроки против первого и первый
      против сцены (`runity::save::diff`)
- [x] Окно сейвов — вкладка Saves: сейвы из папки игры и папок игроков
      редактора, открыть и удалить
- [x] Список систем и их порядок — вкладка Systems: порядок из `step`
      игры, цена каждой системы из отчёта (шаблон меряет каждую)

## 6. Вертикальный срез

- [ ] Одна плейтест-сцена «Дачи» на 2–4 игроков: взять и нести двумя руками,
      дверь на петле, грядка с лопатой, мышь на навигации
- [ ] Список того, чего не хватило на срезе, — сюда

## 7. Код и тесты

- [ ] Переписать движок «Дачи» (~39 тыс. строк C#) там, где runity его ещё не
      повторяет; Morpeh → hecs, DI и шина → системы и события
- [ ] Переписать игру (~104 тыс. строк C#)
- [ ] Перенести тесты (~3 100): сетевые на петле, плохая связь, headless-мир
