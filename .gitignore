target/
**/*.rs.bk

# Built by tools/package-macos.sh.
/dist/

# Screenshots written by the examples into the working directory.
/*.png

# Written by a failing golden-image test, next to the reference it failed against.
*.actual.png
*.diff.png

# The cpu/gpu/diff triptych a failing differential test leaves behind.
*.cpu.png
*.gpu.png
crates/runity-gpu/tests/diff/

# The shader library tools/package-macos.sh compiles into the bundle. The MSL
# source in the tree is the only copy that is committed.
*.metallib

# The editor window's own layout: panel sizes and the open tab, per person.
**/.runity/studio.ron
__pycache__/
