schema_version = "1.0.0"

id = "scrap"
version = "0.1.0"
name = "scrap"
tagline = "Build scrap scenes in Blender: saved files import themselves"
maintainer = "scrap"
type = "add-on"
blender_version_min = "4.2.0"
license = ["SPDX:MIT"]
