schema_version = "1.0.0"

id = "runity"
version = "0.1.0"
name = "runity"
tagline = "Build runity scenes in Blender: saved files import themselves"
maintainer = "runity"
type = "add-on"
blender_version_min = "4.2.0"
license = ["SPDX:MIT"]
